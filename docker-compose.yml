# Review: This compose file sets up a FastAPI application in a Docker container. It is simple, consider adding a volume for code persistence and more services.
services:
  fastapi:
    build: .
    container_name: cloud_computing_fastapi
    ports:
      - "8000:8000"
    restart: always

